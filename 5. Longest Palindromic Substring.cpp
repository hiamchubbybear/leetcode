#include <cstddef>
#include <iostream>
#include <algorithm>
using namespace std;
// Sliding windows
// Not solved
class Solution {
public:
    string longestPalindrome(string s) {
        string ans;
        size_t size = s.size();
        ans.insert(s.begin(),2);
        for(int i = 0 ; i< size ;i ++) {
          if(
        }
    }
};
