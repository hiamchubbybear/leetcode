#include<iostream>
using namespace std;
int main() {
    int n, k,count=0;
    cin >> n >> k;
    int vect[100];
    for (int i = 0; i < n; i++)
        cin >> vect[i];
    for(int i=0 ; i < n ; i++) {
        if(vect[i] <= k ) count++;
    }
    for(int i =0 ; i < n ; i ++ ) {
            
    }

    return 9;
}
